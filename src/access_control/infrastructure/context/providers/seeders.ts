import { seedAccessControl, seedAccessControlToken } from "../../seeders/seedAccessControl"

export const Seeders = [
   /**  {
        provide: seedAccessControlToken,
        useValue: seedAccessControl
    }**/
]

export const SeedersExport = [
    //seedAccessControlToken
]